export interface TableSectionViewSpecs {
  numberOfRows: number;
  numberOfColumns: number;
  colWidths: string[];
  rowHeights: string[];
  rowSpan: number[];
  colSpan: number[];
  visible: boolean[];
}
